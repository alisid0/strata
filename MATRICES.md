# Matrices — topic spine

A new standalone topic (broader than the existing Coordinate Geometry Tier 15,
which only covers matrices-as-transformations). Internal index prefix: **MX#**
(temporary, same convention as CG#/EL# — the real term is BB once locked).

## Tier 0 — Matrix algebra fundamentals (MX1–7)

1. A matrix is just a grid of numbers
2. Rows and columns — describing a matrix's size (m × n)
3. Adding two matrices — add the matching positions
4. Scalar multiplication — multiply every entry by one number
5. Matrix multiplication — why it isn't just "multiply matching positions"
6. Worked example: multiplying two small matrices
7. The identity matrix — multiplying by it changes nothing

## Tier 1 — Matrices as transformations, and what undoes them (MX8–15)

8. A matrix as an instruction for moving every point on the grid
9. Scaling — stretching the whole plane
10. Rotation — turning the whole plane about the origin
11. Reflection — flipping the plane across a line
12. The determinant — how much a transformation stretches area
13. A zero determinant — when a transformation flattens the plane (no inverse)
14. The inverse matrix — the instruction that undoes another instruction
15. Solving simultaneous equations using a matrix

Status: drafted in Editor Mode (batch), not yet run through the real Loop with the
author. Supersedes the old Tier 15 plan inside COORDINATE-GEOMETRY.md (CG188-199,
transformations-only) with a more complete arc covering algebra fundamentals first.
That old entry is left as-is, unused, rather than deleted.
